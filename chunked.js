function chunnk(array, size)
{
    let chunked=[];
  for (let element of array) {
      let last=chunked[chunked.length-1];
    =>chunked[[1,2]]=>[1[1,2]
      if(!last || last.length==size)=>2==2
      {
          chunked.push([element]);=> [[1,2][3]]
          console.log(chunked.push([element]));
      }
      else{
          last.push(element);[1,2]
      }

}
console.log(chunked);
chunked([1,2,3],2);