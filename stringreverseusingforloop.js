function reverse(str){
    let reverse="";
    for(let char of str){
        reverse=char+reverse; //adding on its starting so that it would be able to add and if we add in the add then it will come as usual
        }
        console.log(reverse);
}
reverse("manvi");